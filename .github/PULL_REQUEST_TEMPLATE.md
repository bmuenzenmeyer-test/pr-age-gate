## What and why

## Checklist

- [ ] `vlt run typecheck` passes
- [ ] `vlt run test` passes, and new behavior has a test (see CONTRIBUTING.md)
- [ ] README updated if this changes `action.yml` inputs, CLI flags, or the library's public API
- [ ] No new runtime dependency added without discussing it first (see "Why no dependencies" in the README)
