---
name: Bug report
about: Something isn't working as expected
labels: bug
---

**Using it as:** Action / CLI / library (delete the ones that don't apply)

**What happened**

**What you expected**

**Repro**

- `min-hours` / `bypass-labels` / `bypass-paths` values in use, if relevant
- Command run (CLI) or workflow snippet (Action), with any secrets/tokens redacted
- Node version (`node --version`) if running the CLI/library locally

**Output**

Paste the actual output/error, or the check run's title+summary if it's an Action issue.
